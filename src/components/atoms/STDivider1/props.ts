import { BoxProps, DividerProps, StackProps } from "@chakra-ui/react";

export interface STDivider1Props {
    hStackProps?: StackProps;
    leftLineProps?: DividerProps;
    centerDiamondProps?: BoxProps;
    rightLineProps?: DividerProps;
}
